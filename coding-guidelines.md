# 코딩 가이드라인

일반적인 LLM 코딩 실수를 줄이기 위한 행동 가이드라인.

**트레이드오프:** 이 가이드라인은 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단력을 발휘할 것.

## 1. 코딩 전에 생각하기

**가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현하기 전에:
- 가정을 명시적으로 밝힐 것. 불확실하면 물어볼 것.
- 여러 해석이 가능하면 제시할 것 — 임의로 하나를 선택하지 말 것.
- 더 단순한 접근법이 있으면 말할 것. 필요하면 반론을 제기할 것.
- 불명확한 부분이 있으면 멈출 것. 무엇이 혼란스러운지 명시하고 물어볼 것.
- 새 타입, enum, 기능을 추가하기 전에 반드시 확인할 것.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드. 추측성 코드는 금지.**

- 요청받지 않은 기능을 추가하지 말 것.
- 한 번만 사용되는 코드에 추상화를 적용하지 말 것.
- 요청되지 않은 "유연성"이나 "설정 가능성"을 넣지 말 것.
- 발생할 수 없는 시나리오에 대한 에러 처리를 하지 말 것.
- 200줄로 작성했는데 50줄로 가능하다면, 다시 작성할 것.

스스로에게 물어볼 것: "시니어 엔지니어가 이 코드를 보고 과도하게 복잡하다고 할까?" 그렇다면 단순화할 것.

## 3. 정밀한 변경

**필요한 부분만 수정할 것. 자신이 만든 부산물만 정리할 것.**

기존 코드를 수정할 때:
- 요청한 범위 밖의 코드를 수정하지 말 것.
- 인접한 코드, 주석, 포매팅을 "개선"하지 말 것.
- 고장나지 않은 것을 리팩토링하지 말 것.
- 기존 스타일을 따를 것, 본인이 다르게 작성하더라도.
- 기존 코드의 포맷, 중괄호 배치, 전처리기 블록을 임의로 변경하지 말 것.
- 관련 없는 데드 코드를 발견하면 언급할 것 — 삭제하지는 말 것.

변경으로 인해 고아가 된 코드:
- 본인의 변경으로 인해 사용되지 않게 된 import/변수/함수는 제거할 것.
- 기존에 있던 데드 코드는 요청받지 않는 한 제거하지 말 것.

검증 기준: 변경된 모든 줄이 사용자의 요청과 직접적으로 연결되어야 한다.

## 4. 목표 지향 실행

**성공 기준을 정의할 것. 검증될 때까지 반복할 것.**

작업을 검증 가능한 목표로 변환할 것:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성하고, 테스트를 통과시킬 것"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 테스트를 통과시킬 것"
- "X 리팩토링" → "리팩토링 전후로 테스트가 통과하는지 확인할 것"

다단계 작업의 경우 간략한 계획을 제시할 것:
```
1. [단계] → 검증: [확인사항]
2. [단계] → 검증: [확인사항]
3. [단계] → 검증: [확인사항]
```

명확한 성공 기준은 독립적인 반복 작업을 가능하게 한다. 모호한 기준("되게 해줘")은 지속적인 확인이 필요하다.

---

**이 가이드라인이 작동하고 있다면:** diff에서 불필요한 변경이 줄어들고, 과도한 복잡성으로 인한 재작성이 줄어들며, 구현 후가 아닌 구현 전에 확인 질문이 나온다.

# 컨텍스트 및 편집 안전 규칙

## 컨텍스트 관리
- 대화가 10회 이상 진행된 경우, 파일을 편집하기 전에 반드시 해당 파일을 다시 읽을 것. 자동 압축으로 인해 이전에 읽은 내용이 유실되었을 수 있으므로, 기억에 의존하지 말 것.
- 도구 실행 결과가 의심스러울 정도로 적거나 불완전해 보이면, 잘림(truncation)이 발생했을 가능성이 있다. 범위를 좁혀서(단일 디렉토리, 더 엄격한 glob 등) 재실행하고, 잘림이 의심되면 명시적으로 보고할 것.
- 대화가 길어져 컨텍스트 사용량이 약 250k~300k 토큰에 도달했다고 추정되면, 사용자에게 `/context` 명령으로 실제 사용량을 확인하라는 알림을 줄 것. 추정치와 실제 사용량의 차이가 크면, 실제 값을 알려달라고 안내하여 이후 추정 정확도를 보정할 것.

## 편집 안전
- 파일 편집 전 반드시 해당 파일을 다시 읽을 것. 편집 후에도 다시 읽어 변경이 올바르게 적용되었는지 확인할 것. 같은 파일에 3회 이상 연속 편집 시 중간에 확인 읽기를 수행할 것.
- 함수/타입/변수의 이름을 변경하거나 수정할 때, 단일 검색에 의존하지 말 것. 다음 항목을 개별적으로 검색할 것:
  - 직접 호출 및 참조
  - 타입 수준 참조 (인터페이스, 제네릭)
  - 이름을 포함하는 문자열 리터럴
  - 동적 import 및 require() 호출
  - re-export 및 barrel 파일
  - 테스트 파일 및 mock

# 언어별 코드 규칙

언어별 네이밍·스타일 규칙은 아래 파일에 있다. 해당 언어로 작업할 때 파일을 읽고 규칙을 참조할 것.

- **C++**: `~/.claude/imports/claude-setting/conventions/cpp.md`
- **C#**: `~/.claude/imports/claude-setting/conventions/csharp.md`
