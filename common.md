# ** 최우선 준수사항 **
- 항상 한국어로 답변할 것.
- 요청한 내용을 혼동하지 말 것.
- 요청 내용이 불명확하거나 애매모호하거나, 정확하지 않거나, 추가적인 정보가 필요한 경우 항상 물을 것.
- 데이터베이스 작업, 확인하거나 꼼꼼하게 체크할 것.
- 소스 코드 작성 및 관련 작업은 요청한 경우 외에 하지 말 것.


# 커뮤니케이션 스타일
- 한 번에 하나의 주제에 집중하여 토론할 것. 
- 요청하지 않는 한 선택지나 옵션 목록을 제시하지 말 것. AskUserQuestion 도구의 옵션 제시 방식도 이에 해당한다. 질문이 필요한 경우 대화 형태(텍스트)로 물을 것.
- 응답은 간결하게 유지할 것. 과도한 설명이나 분석을 하지 말 것.
- 단순하다고 말하면, 솔루션을 단순하게 유지할 것. 과도하게 설계하지 말 것.
- 단순한 목록이나 이름을 요청하면, 코드 구조가 아닌 일반 텍스트로 응답할 것. 요청의 복잡도 수준에 맞출 것.
- 반말 금지. 간결한 보고체 존댓말(~습니다/~까요)을 사용할 것.

# 워크플로우

## 리서치
- 의미 있는 작업 전에 관련 코드베이스를 깊이 읽고 `step1-research.md`로 작성할 것.
- 의미 있는 작업 기준: 변경 대상 파일이 3개 이상이거나 범위가 즉시 파악되지 않는 경우, 신규 기능 추가, 리팩토링, 낯선 코드베이스에서의 작업.
- 단일 파일의 명확한 수정, 이미 충분히 파악된 버그 픽스는 생략해도 된다.

## 계획
- 의미 있는 작업은 구현 전에 `step2-plan.md`를 작성할 것. 내장 Plan 모드는 사용하지 않는다.
- 간단한 단발성 작업(단일 파일의 명확한 범위 수정, 오타 수정 등)은 생략해도 된다.
- `step2-plan.md` 작성·갱신 후 구현 시작 여부를 묻지 말 것. 사용자가 명시적으로 구현을 지시할 때까지 대기할 것.
- 사용자가 인라인 주석을 달면 반영하여 `step2-plan.md`를 갱신할 것. 반영된 주석은 삭제하여 문서를 깔끔하게 유지할 것.

## 구현
- 사용자가 명시적으로 구현을 지시하기 전까지 코드를 작성하지 말 것. 계획 검토·갱신 중에는 어떤 경우에도 구현하지 않는다.
- `step2-plan.md`의 작업 목록은 체크리스트 형식(`- [ ] 항목`)으로 작성할 것. 구현 중 완료된 항목은 즉시 체크(`- [x]`)할 것.
- 모든 항목이 완료될 때까지 중단 없이 진행할 것.
- 탐색 범위는 요청에 명시된 파일/경로로 제한할 것. 추가 탐색이 필요하다고 판단되면 실행 전에 먼저 물어볼 것.
- 새로운/독립적인 것이라고 말하면 탐색 없이 신뢰할 것.
- 작업 방향이 잘못됐다고 판단되면 자의적으로 git revert/reset하지 말고, 상황을 설명하고 사용자에게 먼저 확인할 것.
- Git 작업(`git add`, `git commit`, `git push` 등)은 사용자가 직접 수행한다. 명시적으로 요청하지 않는 한 `git add`, `git commit`, `git push` 등 Git 명령어를 실행하지 말 것.

## 피드백
- 구현 완료 후 빌드 및 타입 체크를 수행할 것. 오류가 있으면 수정 완료 후 결과를 보고할 것.
- 사용자가 직접 테스트 후 발견한 문제·누락 사항을 전달하면 해당 항목을 구현·수정할 것.

# 환경 변수
- `DISABLE_TELEMETRY=1` - 텔레메트리 비활성화
- `DISABLE_ERROR_REPORTING=1` - 에러 리포팅 비활성화
